---
includes:
  - has_description.yaml
  - has_namespace.yaml
  - has_json_indent.yaml
  - has_volatile.yaml

required: [fields]

properties:
  codec:
    type: boolean
    default: true

  stream:
    type: boolean
    default: true

  fields:
    type: array
    items:
      $ref: package://ifgen/schemas/StructField.yaml
