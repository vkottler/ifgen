---
includes:
  - has_description.yaml
  - has_volatile.yaml

required: [name, type]

properties:
  name: &pattern_string
    type: string
    pattern: &pattern "^[a-zA-Z0-9-_.:]+$"

  type: *pattern_string

  fields:
    type: array
    items:
      $ref: package://ifgen/schemas/BitField.yaml
