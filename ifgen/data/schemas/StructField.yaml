---
includes:
  - has_description.yaml
  - has_volatile.yaml
  - has_expected_size.yaml

required: [name, type]

properties:
  name: &pattern_string
    type: string
    pattern: &pattern "^[a-zA-Z0-9-_.:]+$"

  type: *pattern_string

  array_length:
    type: integer
    minimum: 2

  fields:
    type: array
    items:
      $ref: package://ifgen/schemas/BitField.yaml
